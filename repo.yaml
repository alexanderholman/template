# filename: repo.yaml
# Minimal repository manifest for the Agent Factory system (flat, append-only files; flexible directory hierarchy).

repo:
  name: "agent-factory"
  hierarchy: "flexible"
  required_files:
    - "agents.yaml"
    - "agents.md"
    - "specs.md"
  optional_files:
    - "project_context.md"
    - "agent_runs.md"
    - "decisions.md"
    - "{specialism}.md"
  logs:
    run_log: "agent_runs.md"
    decision_log: "decisions.md"
  conventions:
    tags_required: ["[SPEC]", "[ASSUMPTION]", "[RISK]", "[TODO]", "[DECISION]", "[TEST]", "[DONE]"]
    append_only_files: ["specs.md", "agent_runs.md", "decisions.md"]
